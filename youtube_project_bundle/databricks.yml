# This is a Declarative Automation Bundle definition for youtube_project_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: youtube_project_bundle
  uuid: 9bb75141-4e00-4451-a7ca-0b2a15d7f337

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405608483030683.3.azuredatabricks.net
    variables:
      catalog: youtube_dev
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://adb-7405608483030683.3.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/dsr9935@outlook.com to make sure we only have a single copy.
      root_path: /Workspace/Users/dsr9935@outlook.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: youtube_prod
      schema: prod
    permissions:
      - user_name: dsr9935@outlook.com
        level: CAN_MANAGE
